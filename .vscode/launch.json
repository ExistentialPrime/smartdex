{
	// Use IntelliSense to learn about possible attributes.
	// Hover to view descriptions of existing attributes.
	// For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch localhost",
			"type": "chrome",
			"request": "launch",
			"url": "http://localhost:4200",
			"webRoot": "${workspaceFolder}/wwwroot",
			"userDataDir" : "${workspaceRoot}/.vscode/chrome"
		},
		/*{
			"name": "Attach to Chrome",
			"type": "chrome",  // Attach Requires chrome to be launched with 
			"request": "attach",  // external debugging enabled first and only works
			"port": 9222,	// when all chrome is shut down first then debug shortcut
      "url": "http://localhost:4200/", // is opened first
			"webRoot": "${workspaceFolder}",
			"sourceMaps": false
		},*/
	]
}
